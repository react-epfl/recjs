var helper = require("test_helper")
  , gently = new (require('gently'))
  , should = require('should')

function defaultModel(app) {
  return helper.model(app, "bundle")
}


describe('bundle', function () {
  it('should ', function () {
  })
})

